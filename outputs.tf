# Nenhum output declarado ainda. Quando o banco existir, este arquivo expõe
# o que os repositórios `oficina-mecanica-lambda-auth` e
# `oficina-mecanica-app` precisam consumir via `terraform_remote_state`
# (ex.: endpoint/porta do banco, ARN do secret com as credenciais) — nunca a
# senha em texto puro.
