name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: Terraform Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.15.8"

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init -backend=false

      - name: Terraform Validate
        run: terraform validate

  # ---------------------------------------------------------------------
  # apply:
  #   name: Terraform Apply
  #   runs-on: ubuntu-latest
  #   needs: validate
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: "1.15.8"
  #     - run: terraform init
  #     - run: terraform apply -auto-approve
  #
  # TODO: habilitar quando existirem (1) a decisão de nuvem (RFC-002,
  # pendente no repositório da aplicação), (2) o backend remoto do state
  # (bucket S3 + DynamoDB, ou equivalente do provedor escolhido) e
  # (3) credenciais/role OIDC configuradas como secret neste repositório.
  # Nenhuma dessas três coisas existe hoje.
  # ---------------------------------------------------------------------
